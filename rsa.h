#ifndef RSA_H_
#define RSA_H_

#include <stdio.h>
#include <stddef.h>
#include <unistd.h>
#include <string.h>
size_t get_least(unsigned long long int n);
void print_product(unsigned long long int n);
#endif
